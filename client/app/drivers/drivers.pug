header#banner.hero-unit
  .container
    h1 Drivers
    p.lead This is the Drivers view

.container
  .row
    .col-lg-12
      h1.page-header Drivers:
      ul.nav.nav-tabs.nav-stacked.col-md-4.col-lg-3.col-sm-6(
        ng-repeat="driver in $ctrl.driversList"
      )
        li
          a(href="#", uib-tooltip="{{driver.nationality}}")
            | {{ driver.forename }} {{ driver.surname }}
